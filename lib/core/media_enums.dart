enum ViewMode { grid, list }
enum SortMode { name, date, size, duration }
enum AppTheme { neon, minimal, cinematic, custom }
enum BrowseMode { allFolders, folders, files }

enum BackgroundPlayMode { off, audioOnly, pip, askEveryTime }
enum RepeatMode { off, one, all }
enum DecoderMode { auto, hardware, software }
enum AspectRatioMode { fit, fill, stretch, sixteenNine, fourThree, original }
